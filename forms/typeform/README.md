# Typeform application (API helper)

**Live form (already published):** https://form.typeform.com/to/old8S3IG  

Do not run [`create.sh`](create.sh) unless you intend to create a **new** Typeform copy. The live form was built in the Typeform admin UI with the SBC logo on the welcome screen.

- Field notes: [`../../notes/typeform-application.md`](../../notes/typeform-application.md)
- Payload reference: [`application.json`](application.json)
- Recreate via API only with `TYPEFORM_TOKEN` set
